---
title : samba
---

```
apt-get source samba
```

```
apt-get install linux-headers-`uname -r`
apt-get install build-essential gawk vim
apt-get install libldap2-dev libkrb5-dev libwbclient-dev libtalloc-dev libpopt-dev libtdb-dev
```
